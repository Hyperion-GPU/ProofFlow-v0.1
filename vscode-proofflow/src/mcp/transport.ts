/**
 * @deprecated The stdio MCP transport has been removed. The extension now
 * talks to the ProofFlow backend directly over REST through {@link McpClient}.
 * This file is retained as an empty stub so external imports fail loudly
 * rather than silently load a broken implementation.
 */
export const STDIO_TRANSPORT_REMOVED = true;
